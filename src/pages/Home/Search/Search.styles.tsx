import styled from '@emotion/styled'

export const SearchContaeinr = styled.div`
  width: calc(100% - 15px);

  fieldset {
    cursor: pointer;
  }

  div {
    right: 15px;
  }
`
export const SearchTitle = styled.div`
  margin-left: 10px;
  font-size: 1.5em;
  font-weight: bold;
  margin-top: 20px;
  margin-bottom: 20px;

  b {
    color: #ff820a;
  }
`
